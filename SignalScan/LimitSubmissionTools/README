Usage:
------
./submit <path to directory containing all limit input files from the MakeMultiChannelInputs tool>
copy&paste the `qsub ...` command into the shell

./fetch <job directory 2011-11-...> creates a tar file containing all limit results which can be cp'ed to the PlotScript directory for plotting

Orig.sh is the script each batch job runs

limit is the script to calculate asymtotic and hybrid CLs observed and expected limits using the HiggsAnalysis/CombinedLimit tool.
